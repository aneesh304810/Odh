[global]
index-url = https://nexus.bbh.com/repository/pypi-proxy/simple/
trusted-host = nexus.bbh.com
timeout = 120

[install]
# Allow --user installs inside container (needed because $HOME may be
# owned by the arbitrary UID OpenShift assigns at runtime).
user = true
