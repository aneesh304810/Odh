# Minimal dbt project used to smoke-test the developer workbench.
# Replace with your real project once the environment is validated.

name: 'odh_dev'
version: '1.0.0'
config-version: 2

profile: 'odh_dev'

model-paths:     ["models"]
analysis-paths:  ["analyses"]
test-paths:      ["tests"]
seed-paths:      ["seeds"]
macro-paths:     ["macros"]
snapshot-paths:  ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  odh_dev:
    example:
      +materialized: view
